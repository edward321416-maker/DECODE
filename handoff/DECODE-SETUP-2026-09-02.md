# Codex → ChatGPT

HISTORICAL SNAPSHOT — records the earlier task only. Current authority and repository state are in [Current Status](../docs/CURRENT_STATUS.md) and [Publication Policy](../docs/PUBLICATION_POLICY.md). Later publication supersedes old local-only/private/pending statements; earlier check counts are not a fresh audit.

Report ID: DECODE-SETUP-2026-09-02 | Scope: operating setup, not experiment execution
Owner: AI/Engineering Lead | Source revision: uncommitted local files; no GitHub commit/PR

Keep every section below in future reports. Replace entries with scoped facts and evidence, or explicitly say NOT YET TESTED. Preserve previous run artifacts before updating this current handoff.

## IMPLEMENTED

Operating structure, planning documents, candidate dataset/protocol specifications, reserved schema/sample/result directories, experiment-log header, two-way handoffs and project prompt policies. Annotation application, machine schema and model are NOT IMPLEMENTED in this setup.

## ACTUAL TEST

NOT YET TESTED — no consented real VOD/expert test session executed. Cases, annotation time, agreement and coverage: unavailable. Future report: run ID; consent/source references; expert roles; actual n; method; measurements; evidence paths; limitations.

## SELF-BENCHMARK

Documentation integrity verification only; see [SETUP_VERIFICATION](../docs/SETUP_VERIFICATION.md). No model benchmark or independent expert evaluation. Future report: internal evaluator, dataset origin, method, command, outcome and reproducibility reference.

## SIMULATED

NOT TESTED — no synthetic decision fixtures or simulated stress run created in this setup. Future report: mark every synthetic artifact SIMULATED; report software behavior only; never promote to ACTUAL TEST.

## FAILED

No executed stress-test failures to report. GitHub publication and Google synchronization are BLOCKED by missing destination/configuration, not failed experiments. Future report: exact failed check, reproduction, observed error and remaining impact; redact secrets.

## NOT TESTED

③-A-1 actual stress test; annotation/schema tool; model behavior; Runtime; builds; expert timing/agreement; principle coverage; context sufficiency; coaching benefit; Google API writes; empirical prompt robustness/token savings. Manifest scripts were not executed.

## FILES CHANGED

Use a unified diff/patch or exact SEARCH/REPLACE, never whole-file dumps. For this setup, the delivered `decode-operating-setup.diff` is the authoritative change report; it includes only task changes against captured pre-edit files, not pre-existing untracked app code. Future report: attach the reviewed diff or PR patch and its base revision; note any unrelated dirty state preserved.

## RECOMMENDED NEXT DECISION

User: provide the existing DECODE GitHub URL or explicitly approve creating one. Product: approve the versioned implementation request and settle candidate nullability/timing/aggregation choices; supply consented VOD and experts before ACTUAL TEST. Google logging remains pending until approved spreadsheet/folder/authentication are available. Do not authorize Alpha 50/150 yet.
