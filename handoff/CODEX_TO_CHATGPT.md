# Codex → ChatGPT

Report ID: DECODE-DRY-READINESS-IMPLEMENTATION-CORRECTION-2026-09-09 | Scope: PR #19 correction round 1 (8 findings)
Owner: AI/Engineering Lead | Base: `origin/main = 07d5789750d206613c0dec058f392be65cb690ab` (PR #18 merge, unchanged throughout), branch `claude/dry-readiness-implementation`, PR #19 kept OPEN — no new PR, no merge.

Prior report: DECODE-DRY-READINESS-IMPLEMENTATION-2026-09-09, this file's immediately preceding content (superseded by this report). See Git history for the exact prior text.

## IMPLEMENTED

Product's fresh review of PR #19's round-1 head `42d8988f4e77ddbb996de38cdc0588a4b7ea6a2b` returned `REVISE — MERGE NOT AUTHORIZED` against 8 findings. All 8 were corrected in place on PR #19 with genuine RED-before-GREEN TDD, one commit per finding:

1. **D034 frozen hashes were manually-maintained placeholder strings** (`"readiness-source-v1"`, `"foundation-source-v1"`, `"protocol-v1"`, `"gate-catalog-v1"`), never actually derived from the artifacts they claimed to represent — a real change to readiness source, the foundation dependency, the Protocol document, or the gate definitions would never trip `STALE_RUN`. Fixed: `readiness/src/domain/frozen-hash-inputs.ts` now computes all six D034 inputs from real content — sorted directory-content manifests for readiness/foundation source and the gate catalog (`readiness/src/gates/`), the actual Protocol document's bytes, and the canonicalized fixture bytes. Each function accepts an optional directory/path override so tests exercise the exact production code path against controllable temp fixtures without mutating the live tree. The computed Protocol hash (`11b42a0be...23dc2ef0`) independently matches this repo's own previously-verified SHA-256 for that exact file from the M0 authority materialization.
2. **Second Expert relationship-provenance enum was invented, not the real Protocol §8 enum.** Corrected `RelationshipProvenance` to the exact six values: `NONE`, `FORMER_TEAMMATE`, `CURRENT_TEAMMATE`, `FORMER_COACHING_RELATION`, `CURRENT_COACHING_RELATION`, `OTHER`. Added `assertValidRelationshipProvenance()`, wired into `qualifySecondExpertCandidate()`. Remapped all three fixtures' qualification candidates. Added checker guard `readiness-relationship-provenance-exact-enum`.
3. **Canonical verdict enum included an invented `POOR` value, and directional agreement was computed from a pre-baked AGREE/DISAGREE field** rather than real Founder/Second-Expert verdict pairs. Corrected `GoldVerdictLabel` to the exact six canonical verdicts (`OPTIMAL`/`ACCEPTABLE`/`SUBOPTIMAL`/`ERROR`/`UNCERTAIN`/`INSUFFICIENT_CONTEXT`). Replaced `computeDirectionalAgreement(outcomes)` with `computeDirectionalAgreementFromPairs(pairs)` over exactly 4 planned Second Expert pairs (Protocol §16), reporting eligible pairs (excluding UNCERTAIN/INSUFFICIENT_CONTEXT on either side), directional (category-level: positive=OPTIMAL/ACCEPTABLE, negative=SUBOPTIMAL/ERROR) agreement, exact-verdict matches/disagreements separately, and completed/planned coverage with an explicit `INCOMPLETE_SECOND_EXPERT_COVERAGE` reason when a pair hasn't completed — never silently dropped or coerced to zero.
4. **Operational rehearsal results lacked explicit provenance on the artifact itself, and Protocol §23's Pilot Operator checklist did not exist.** Added a shared `RehearsalArtifact` marker (`{rehearsal:true, evaluationMode:"SELF_BENCHMARK", dataOrigin:"SIMULATED"}`) to `ConsentRehearsalResult`, `SourceRightsResult`, and `QualificationResult`. Implemented `rehearsePilotOperatorChecklist()` with the exact 12 required categories (age/status, VOD source/right, ACTUAL TEST purpose, retention scope, dataset retention choice, evaluation reuse choice, model-training choice, withdrawal notice, guardian consent, participant assent, guardian verification, unresolved legal/privacy blocker); any missing item blocks it, with deliberately no override input. Wired into the `consent-guardian-assent-rehearsal` mandatory gate.
5. **Production source (`readiness/src/gates/integration-gates.ts`) directly constructed `{evaluationMode:"ACTUAL_TEST", ...}`** to prove Foundation's `validateEvidenceRecord()` rejects it — a forbidden literal inside production code. Moved that negative-mutation proof to a new test-only file (`test/canonical-provenance-anti-promotion.test.ts`). The gate now proves the same two invariants (canonical record validates; `readinessVerdict`/`ExecutionStatus` remain structurally separate fields) without ever writing the literal. The checker's `readiness-no-forbidden-provenance-literal` guard now scans every file under `readiness/src/`, not an arbitrary 3-file subset.
6. **Final evidence/documentation numbers were inconsistent across the PR body, `docs/CURRENT_STATUS.md`, and this file** (1278 vs 1260 vs 1276 checker counts; 120 vs 121 files). Resolved by this single fresh final-head verification (see SELF-BENCHMARK below) — every document below now cites the identical numbers from this one run.
7. **The round-1 dry-run artifact was neither deleted nor left ambiguous.** `readiness/results/readiness_055d93ff-d4e3-4447-9b39-b8e56f4cbcc2.json` is preserved immutably as historical evidence. Recomputing frozen hashes under the corrected implementation and calling `evaluateCurrentReadiness()` against it now correctly reports it `BLOCKED`/`STALE_RUN` — it is historical, not current. A fresh run under the corrected implementation, `readiness_efb4c87a-f762-40b4-842c-2213d40bdc49.json`, is the current evidence.
8. **Scope preserved:** PR #5, ACTUAL TEST, PLAN 1B, 50/150, `foundation/`'s public API, and the three frozen authority documents were not touched — verified below.

## ACTUAL TEST

NOT YET TESTED — no consented real VOD or independent expert session was run for this correction round, the implementation, PR-A, the design spec, or DECODE generally. `ACTUAL TEST = NOT YET TESTED`. No artifact produced by this correction round constitutes ACTUAL TEST evidence.

## SELF-BENCHMARK

One fresh final-head verification, run after all 8 findings were corrected, and cited identically everywhere below:

- Start-gate: fresh-fetched `origin/main` and the PR #19 branch; both matched exactly (`07d5789750d206613c0dec058f392be65cb690ab`, `42d8988f4e77ddbb996de38cdc0588a4b7ea6a2b`) before any correction began.
- Authority blob re-verification: PLAN 1A `bfb5e35b921ccc320f3ffb2631b661368206fa6b`, Integrated Spec `f7571338e93a408a8aeef93d63275d7076e76f80`, 10-Case ACTUAL TEST Protocol `4d7788bb39d68c5cd147408a85954cd5a0e7b8f0` — all three unchanged. `foundation/src/index.ts` (`b07c48e5...460784`) and `foundation/package.json` (`a644bdaa...ea9733e`) verified byte-unchanged by exact SHA-256.
- Fresh clean-install verification (`rm -rf node_modules && npm ci`):
  - `readiness/`: 0 vulnerabilities, `npm run typecheck` 0 errors, `npm test` **119/119 PASS**.
  - `foundation/`: 0 vulnerabilities, `npm run typecheck` 0 errors, `npm test` **82/82 PASS** — unchanged from every prior baseline, no regression.
- `node scripts/check-operating-docs.mjs` (default): **1336/1336 PASS.**
- `node scripts/check-operating-docs.mjs --index`: **1466/1466 PASS.** This same verification pass caught a genuine inventory gap — 7 test files created during this correction round (`readiness/src/rehearsal/artifact.ts`, `pilot-operator-checklist.ts`, and 5 new test files) had never been added to `docs/PUBLICATION_FILES.json` — fixed before this count.
- `node scripts/check-operating-docs.mjs --tracked` (post-commit): reported in the return summary for this correction round.
- `node scripts/check-operating-docs.semantic.test.mjs`: **60/60 PASS** — 1 new RED-before-GREEN guard this correction round (exact relationship-provenance six-value enum), added to the 5 guards from the prior implementation round (`foundation/` index/package byte-unchanged ×2, exact mandatory gate-ID catalog, exact CLI flag set, run-artifact schema not collapsed, no forbidden provenance literal — that last guard's scan was also widened this round from 3 files to the entire `readiness/src/` tree, reusing the existing check id). Each new guard's RED was confirmed genuinely (the check id did not exist, or the mutation's target text did not match) before implementation.
- One real DRY harness execution under the corrected implementation, normal CLI wiring (no qualifying local STT adapter): `readiness_efb4c87a-f762-40b4-842c-2213d40bdc49` — `readinessVerdict=BLOCKED`, `ExecutionStatus=BLOCKED`, gate `local-transcription-port=NOT_EXECUTED/LOCAL_TRANSCRIPTION_UNAVAILABLE`, all other 21 mandatory gates `PASS`, `end-to-end-synthetic-rehearsal=PASS`. Frozen hashes: `readinessSource=dd8a2429...c862d`, `foundationSource=5b051d99...27c7ae` (identical to round 1 — `foundation/` untouched), `protocol=11b42a0b...c2ef0` (identical — matches the independently-verified M0 SHA-256), `schema=92521fc3...9c1305`, `fixtureSet=861d8f3e...48e0ee`, `gateDefinition=a38454a1...374166`.
- The round-1 artifact `readiness_055d93ff-...bcc2` re-evaluated against the corrected implementation's freshly-recomputed hashes: `evaluateCurrentReadiness()` returns `BLOCKED`/`STALE_RUN` — confirmed historical, not current.
- `git diff --cached --check`: exit 0.
- `git diff 07d5789750d206613c0dec058f392be65cb690ab..HEAD -- foundation/` and the same for each of the three frozen authority documents and PLAN 1A: empty (verified).
- `docs/PUBLICATION_FILES.json` → version 8, **129 files** (round 1's 121, plus 7 test files this correction round had omitted from the inventory, plus the new round-2 run artifact).
- This is SELF-BENCHMARK/documentation-check/genuine-test evidence, not ACTUAL TEST evidence.

## SIMULATED

Same three versioned fixture sets as round 1 (`full-ready-v1`, `no-local-transcription-v1`, `replacement-unavailable-v1`), updated in this round for the corrected relationship-provenance enum, canonical verdict enum, `secondExpertPairs` (Protocol §16, replacing the old per-case `directionalAgreement` field), and `pilotOperatorChecklistCompletedItems`. Still all `EvaluationMode=SELF_BENCHMARK`, `DataOrigin=SIMULATED`, `rehearsal:true`. No real VOD, PII, or actual consent/rights/qualification data anywhere.

## FAILED

None as final state. Every RED in this correction round was confirmed genuine (missing export, module load failure, or an assertion failing for the stated reason) before its fix, then reverified GREEN. One meaningful-RED sanity check was performed on the Pilot Operator checklist gate wiring (temporarily stubbing the check to `if (false)`, confirming the integration test caught it, then reverting) to prove the new integration test is load-bearing, not tautological.

## NOT TESTED

ACTUAL TEST evidence of any kind; PLAN 1B; a concrete local STT adapter (deliberately out of scope, D036); expert usability; coaching effectiveness.

## FILES CHANGED

This correction round (5 commits, one per code finding, plus this reconciliation commit): `readiness/src/domain/frozen-hash-inputs.ts`; `readiness/src/fixtures/contracts.ts`; `readiness/src/protocol/measurement.ts`; `readiness/src/gates/metric-gates.ts`; `readiness/src/gates/operational-gates.ts`; `readiness/src/gates/integration-gates.ts`; `readiness/src/rehearsal/consent.ts`, `source-rights.ts`, `second-expert-qualification.ts`; new `readiness/src/rehearsal/artifact.ts` and `pilot-operator-checklist.ts`; all three `readiness/fixtures/v1/*.json`; new test files `frozen-hash-provenance.test.ts`, `relationship-provenance.test.ts`, `pilot-operator-checklist.test.ts`, `production-source-safety.test.ts`, `canonical-provenance-anti-promotion.test.ts`; updated `measurement-timing.test.ts`; `scripts/check-operating-docs.mjs` and `scripts/check-operating-docs.semantic.test.mjs`; `docs/PUBLICATION_FILES.json` (new round-2 artifact); `docs/CURRENT_STATUS.md`; both handoffs; `experiments/ai_execution_log.pending.csv`; new committed artifact `readiness/results/readiness_efb4c87a-f762-40b4-842c-2213d40bdc49.json`. `readiness/results/readiness_055d93ff-...bcc2.json` (round 1) preserved unchanged. No change to `foundation/`, the Integrated Spec, PLAN 1A, or the 10-Case ACTUAL TEST Protocol. PR #5 untouched.

## RECOMMENDED NEXT DECISION

Product performs a fresh independent review of PR #19's corrected exact final head against these 8 findings. PR #19 must not be merged by Engineering — it remains OPEN. If Product finds the corrections acceptable, Product may grant merge approval; if not, Product returns further specific findings, or opens a D017 decision interview if a material gap is found. No implementation of a concrete local STT adapter, no PLAN 1B, no ACTUAL TEST, and no 50/150 expansion is authorized by this correction round.
